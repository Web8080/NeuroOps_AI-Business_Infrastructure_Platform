# Prometheus alert rules placeholder. Load in Prometheus or Grafana.

# groups:
#   - name: neuroops
#     rules:
#       - alert: ServiceDown
#         expr: up{job=~"neuroops-.*"} == 0
#         for: 1m
#         labels:
#           severity: critical
#         annotations:
#           summary: "NeuroOps service {{ $labels.job }} is down"
#       - alert: HighErrorRate
#         expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.05
#         for: 5m
#         labels:
#           severity: warning
#       - alert: HighLatency
#         expr: histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m])) > 1
#         for: 5m
#         labels:
#           severity: warning
